# beets-beatport-plugin

This plugin parse HTML data from a search at classic.beatport.com.

The plugin work with the `--singletons` param of beets.

## Dependencies

Python *requests* and *lxml* libraries :
> pip install requests lxml
